# 数据字典

- 共 21 张表

## ads.abc_summary
- 分层：ads；说明：ADS 应用层：ABC 分级汇总（金额帕累托 A<=70% B<=90% C 其余）

| 列名 | 类型 | 说明 |
|---|---|---|
| `abc` | char | ABC 级别 |
| `line_cnt` | int | 交付行数 |
| `total_value` | double | 金额（USD） |
| `value_pct` | decimal | 金额占比% |
| `vendor_cnt` | int | 供应商数 |

## ads.otd_summary
- 分层：ads；说明：ADS 应用层：整体 KPI（OTD/延迟分位/周期中位等，供 BI 卡片）

| 列名 | 类型 | 说明 |
|---|---|---|
| `kpi` | varchar | KPI 名 |
| `kpi_note` | varchar | 说明 |
| `kpi_value` | decimal | 数值 |

## ads.quality_summary
- 分层：ads；说明：ADS 应用层：质量巡检汇总（供 BI 质量页）

| 列名 | 类型 | 说明 |
|---|---|---|
| `dimension` | varchar | 质量维度：完整性/有效性/一致性/及时性 |
| `fail_cnt` | int | 失败数 |
| `layer` | varchar | 分层 |
| `pass_rate` | decimal | 通过率% |
| `total_cnt` | int | 规则数 |

## ads.supplier_scorecard
- 分层：ads；说明：ADS 应用层：供应商绩效评分卡（>=10 行门槛，权重 40/30/20/10）

| 列名 | 类型 | 说明 |
|---|---|---|
| `abc` | char | ABC 分级 |
| `asn_cnt` | int | 发货单数 |
| `avg_late` | decimal | 平均迟到（仅迟交样本，天） |
| `late_cnt` | int | 迟到行数 |
| `line_cnt` | int | 交付行数 |
| `otd` | decimal | 准时率% |
| `score` | decimal | 综合分（0-100） |
| `score_rank` | int | 综合分排名 |
| `total_value` | double | 交付金额（USD） |
| `vendor` | varchar | 供应商 |

## dim.dim_country
- 分层：dim；说明：国家维度

| 列名 | 类型 | 说明 |
|---|---|---|
| `country` | varchar | 国家（自然键） |
| `line_cnt` | int | 交付行数 |
| `vendor_cnt` | int | 供应商数 |

## dim.dim_date
- 分层：dim；说明：日期维度（由交付日期范围生成）

| 列名 | 类型 | 说明 |
|---|---|---|
| `cal_date` | date | 日历日期 |
| `cal_month` | tinyint | 月份 |
| `cal_quarter` | tinyint | 季度 |
| `cal_year` | smallint | 年份 |
| `year_month` | varchar | YYYY-MM |

## dim.dim_product_group
- 分层：dim；说明：产品维度

| 列名 | 类型 | 说明 |
|---|---|---|
| `line_cnt` | int | 交付行数 |
| `product_group` | varchar | 产品大类（自然键） |
| `sub_classification` | varchar | 产品细分 |

## dim.dim_vendor
- 分层：dim；说明：供应商维度

| 列名 | 类型 | 说明 |
|---|---|---|
| `asn_cnt` | int | 发货单数 |
| `first_date` | date | 首次交付日 |
| `last_date` | date | 最近交付日 |
| `line_cnt` | int | 交付行数 |
| `vendor` | varchar | 供应商（自然键） |

## dwd.fact_shipment
- 分层：dwd；说明：DWD 明细层：清洗标准化后的交付明细 + 派生特征

| 列名 | 类型 | 说明 |
|---|---|---|
| `asn_dn_no` | varchar | 发货通知/交货单编号 |
| `brand` | varchar | 品牌 |
| `c_total` | int | 总周期：询价→交付（天） |
| `c1_pq2po` | int | 周期段1：询价→下单（天） |
| `c2_po2sched` | int | 周期段2：下单→计划（天） |
| `c3_sched2deliv` | int | 周期段3：计划→交付（天） |
| `country` | varchar | 送达国家 |
| `delay_days` | int | 延迟天数=实际-计划（负值=提前） |
| `delivered_date` | date | 实际交货日 |
| `delivery_month` | varchar | 交付月份（YYYY-MM） |
| `delivery_year` | int | 交付年份 |
| `dosage` | varchar | 剂量强度 |
| `dosage_form` | varchar | 剂型 |
| `early_bool` | tinyint | 是否提前（delay<0） |
| `first_line_designation` | varchar | 是否一线用药 |
| `flag_freight_outlier` | tinyint | 运费 IQR×1.5 离群 |
| `flag_insurance_outlier` | tinyint | 保险费 IQR×1.5 离群 |
| `flag_line_value_outlier` | tinyint | 货值 IQR×1.5 离群 |
| `flag_negative_cycle` | tinyint | 存在负周期 |
| `flag_pack_price_outlier` | tinyint | 每盒价格 IQR×1.5 离群 |
| `flag_qty_outlier` | tinyint | 数量 IQR×1.5 离群 |
| `flag_unit_freight_extreme` | tinyint | 单位运费>10000 USD/kg |
| `flag_unit_price_outlier` | tinyint | 单价 IQR×1.5 离群 |
| `flag_weight_outlier` | tinyint | 重量 IQR×1.5 离群 |
| `flag_weight_reference` | tinyint | 重量为文本引用（See DN/ASN） |
| `flag_zero_value` | tinyint | 货值为 0 |
| `freight_flag` | varchar | 运费状态：numeric/included/invoiced/reference |
| `freight_numeric` | double | 运费（USD，文本标记已置空） |
| `fulfill_via` | varchar | 配送方式 |
| `id` | int | 唯一键 |
| `insurance` | double | 保险费（USD） |
| `item_description` | varchar | 品名描述 |
| `line_value` | double | 行项目总货值（USD） |
| `managed_by` | varchar | 管理办公室 |
| `manufacturing_site` | varchar | 生产地点 |
| `molecule_test_type` | varchar | 分子式/检测类型 |
| `ot_bool` | tinyint | 是否准时（delay<=0） |
| `pack_price` | double | 每盒价格 |
| `po_date` | date | 下单日（解析后） |
| `po_flag` | varchar | PO 日期状态：date/from_rdc/not_captured |
| `po_so_no` | varchar | 采购/销售订单编号 |
| `pq_date` | date | 询价发出日（解析后） |
| `pq_flag` | varchar | PQ 日期状态：date/pre_pq/not_captured |
| `pq_no` | varchar | 采购询价编号 |
| `product_group` | varchar | 产品大类 |
| `project_code` | varchar | 项目代码 |
| `qty` | double | 行项目数量 |
| `recorded_date` | date | 录入日 |
| `scheduled_date` | date | 计划交货日 |
| `shipment_mode` | varchar | 运输方式 |
| `sub_classification` | varchar | 产品细分 |
| `unit_freight` | double | 单位运费（USD/kg） |
| `unit_price` | double | 单价 |
| `uom` | int | 每包装单位量 |
| `vendor` | varchar | 供应商 |
| `vendor_inco_term` | varchar | 贸易术语 |
| `weight_numeric` | double | 重量（kg，文本引用已置空） |

## dws.cycle_summary
- 分层：dws；说明：DWS 汇总：采购周期分段统计

| 列名 | 类型 | 说明 |
|---|---|---|
| `cycle_seg` | varchar | 周期段：c1/c2/c3/total |
| `median_days` | decimal | 中位数（天） |
| `p95_days` | decimal | P95（天） |
| `sample_cnt` | int | 有效样本数 |
| `seg_desc` | varchar | 周期段描述 |

## dws.monthly_summary
- 分层：dws；说明：DWS 汇总：月度趋势

| 列名 | 类型 | 说明 |
|---|---|---|
| `line_cnt` | int | 交付行数 |
| `ot_cnt` | int | 准时行数 |
| `otd_pct` | decimal | 准时率% |
| `total_value` | double | 交付金额（USD） |
| `year_month` | varchar | 交付月份 |

## dws.otd_by_country
- 分层：dws；说明：DWS 汇总：国家维度时效

| 列名 | 类型 | 说明 |
|---|---|---|
| `country` | varchar | 国家 |
| `line_cnt` | int | 交付行数 |
| `median_delay` | decimal | 延迟中位数（天） |
| `ot_cnt` | int | 准时行数 |
| `otd_pct` | decimal | 准时率% |

## dws.otd_by_mode
- 分层：dws；说明：DWS 汇总：运输方式维度时效

| 列名 | 类型 | 说明 |
|---|---|---|
| `avg_delay` | decimal | 平均延迟（天，全样本含负值） |
| `line_cnt` | int | 交付行数 |
| `median_delay` | decimal | 延迟中位数（天） |
| `ot_cnt` | int | 准时行数 |
| `otd_pct` | decimal | 准时率% |
| `p95_delay` | decimal | 延迟 P95（天） |
| `shipment_mode` | varchar | 运输方式 |

## dws.unit_freight_by_mode
- 分层：dws；说明：DWS 汇总：运输方式单位运费

| 列名 | 类型 | 说明 |
|---|---|---|
| `median_freight` | decimal | 单位运费中位数（USD/kg） |
| `p90_freight` | decimal | 单位运费 P90（USD/kg） |
| `sample_cnt` | int | 有效样本（分子分母均有效） |
| `shipment_mode` | varchar | 运输方式 |

## meta.column_dict
- 分层：meta；说明：元数据：字段字典

| 列名 | 类型 | 说明 |
|---|---|---|
| `column_comment` | varchar | 列注释 |
| `column_name` | varchar | 列名 |
| `data_type` | varchar | 数据类型 |
| `schema_name` | varchar | schema 名 |
| `source_field` | varchar | 来源字段（源 CSV） |
| `table_name` | varchar | 表名 |

## meta.dq_rule
- 分层：meta；说明：元数据：质量校验规则注册表

| 列名 | 类型 | 说明 |
|---|---|---|
| `description` | varchar | 规则说明 |
| `dimension` | varchar | 质量维度 |
| `enabled` | tinyint | 是否启用 |
| `expected` | varchar | 期望值/阈值 |
| `layer` | varchar | 作用分层 |
| `level` | varchar | 级别：fatal/warning |
| `rule_id` | varchar | 规则 ID |
| `rule_name` | varchar | 规则名 |

## meta.table_dict
- 分层：meta；说明：元数据：表字典

| 列名 | 类型 | 说明 |
|---|---|---|
| `layer` | varchar | 分层 |
| `schema_name` | varchar | schema 名 |
| `table_comment` | varchar | 表注释 |
| `table_name` | varchar | 表名 |

## ods.raw_shipment
- 分层：ods；说明：ODS 贴源层：SCMS 交付原始记录（字面值原样保留）

| 列名 | 类型 | 说明 |
|---|---|---|
| `asn_dn_no` | varchar | 发货通知/交货单编号 |
| `brand` | varchar | 品牌 |
| `country` | varchar | 送达国家 |
| `delivered_date_raw` | varchar | 实际交货日（贴源字符串） |
| `dosage` | varchar | 剂量强度 |
| `dosage_form` | varchar | 剂型 |
| `first_line_designation` | varchar | 是否一线用药 |
| `freight_raw` | varchar | 运费（贴源字符串，含文本标记） |
| `fulfill_via` | varchar | 配送方式（Direct Drop / From RDC） |
| `id` | int | 原始记录唯一键（源 CSV ID） |
| `insurance_raw` | varchar | 保险费（贴源字符串） |
| `item_description` | varchar | 品名描述 |
| `line_value_raw` | varchar | 行项目总货值（贴源字符串） |
| `managed_by` | varchar | 管理办公室 |
| `manufacturing_site` | varchar | 生产地点 |
| `molecule_test_type` | varchar | 分子式/检测类型 |
| `pack_price_raw` | varchar | 每盒价格（贴源字符串） |
| `po_date_raw` | varchar | 下单日（贴源字符串） |
| `po_so_no` | varchar | 采购/销售订单编号 |
| `pq_date_raw` | varchar | 询价发出日（贴源字符串） |
| `pq_no` | varchar | 采购询价编号（含 Pre-PQ Process 特殊值） |
| `product_group` | varchar | 产品大类 |
| `project_code` | varchar | 项目代码 |
| `qty_raw` | varchar | 行项目数量（贴源字符串） |
| `recorded_date_raw` | varchar | 录入日（贴源字符串） |
| `scheduled_date_raw` | varchar | 计划交货日（贴源字符串） |
| `shipment_mode` | varchar | 运输方式（含 N/A） |
| `sub_classification` | varchar | 产品细分 |
| `unit_price_raw` | varchar | 单价（贴源字符串） |
| `uom_raw` | varchar | 每包装单位量（贴源字符串） |
| `vendor` | varchar | 供应商 |
| `vendor_inco_term` | varchar | 贸易术语（含 N/A - From RDC） |
| `weight_raw` | varchar | 重量（贴源字符串，含 See DN/ASN 引用） |

## ops.dq_alert
- 分层：ops；说明：质量告警事件表（fatal 级别写入）

| 列名 | 类型 | 说明 |
|---|---|---|
| `alert_id` | bigint | 告警 ID |
| `alert_ts` | datetime | 告警时间 |
| `level` | varchar | 级别 |
| `message` | varchar | 告警消息 |
| `rule_id` | varchar | 规则 ID |
| `run_id` | bigint | 运行 ID |
| `status` | varchar | 状态：open/resolved |

## ops.dq_run_result
- 分层：ops；说明：质量校验结果明细

| 列名 | 类型 | 说明 |
|---|---|---|
| `actual` | varchar | 实际值 |
| `affected_rows` | bigint | 受影响行数 |
| `checked_ts` | datetime | 校验时间 |
| `dimension` | varchar | 质量维度 |
| `expected` | varchar | 期望值 |
| `level` | varchar | 级别 |
| `message` | varchar | 消息 |
| `result_id` | bigint | 结果 ID |
| `rule_id` | varchar | 规则 ID（关联 meta.dq_rule） |
| `run_id` | bigint | 运行 ID（关联 run_log） |
| `status` | varchar | 结果：pass/fail |

## ops.run_log
- 分层：ops；说明：运行日志

| 列名 | 类型 | 说明 |
|---|---|---|
| `message` | varchar | 消息 |
| `rows_affected` | bigint | 影响行数 |
| `run_id` | bigint | 运行 ID |
| `run_ts` | datetime | 运行时间 |
| `stage` | varchar | 阶段：ingest/quality/reconcile/refresh_ads |
| `status` | varchar | 状态：success/fail |
